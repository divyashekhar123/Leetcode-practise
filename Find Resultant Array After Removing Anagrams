from collections import Counter

class Solution:
    def removeAnagrams(self, words):

        result = [words[0]]         

        for i in range(1, len(words)):
            # Keep only if NOT anagram of previous word in result
            if Counter(words[i]) != Counter(words[i-1]):
                result.append(words[i])

        return result
